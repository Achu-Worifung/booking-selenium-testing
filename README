how to run:
clone the repo using git clone https://github.com/Achu-Worifung/booking-selenium-testing.git

navigate into the repo.

install dependencies using the command: npm install

run a test: node mainsearchtest.js
